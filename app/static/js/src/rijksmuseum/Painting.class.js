export default class Painting {
    constructor(id, title, longTitle, imageUrl) {
        this.id = id;
        this.title = title;
        this.longTitle = longTitle;
        this.imageUrl = imageUrl;
    }

}
